{% extends "base_layout_html.twig" %}
{%  block css %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/fruits.css') }}">
{% endblock %}


{% block content %}
    {{ parent() }}

    <ul class="fruit-list">
        {% for fruit in fruitList %}
            <li>
                {% if loop.index is divisible by (2) %}
                <strong>{{ fruit }}</li></strong>
                    {% else %}
                        {{ fruit }}
                    {% endif %}

        {% endfor %}
    </ul>
    <a href="{{ path('hellopage',{age:5}) }}">lien</a>
{% endblock %}